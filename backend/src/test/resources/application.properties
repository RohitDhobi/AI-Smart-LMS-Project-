# Same datasource as the main configuration so the test context can start.
spring.datasource.url=jdbc:mysql://localhost:3306/ai_smart_lms
spring.datasource.username=root
spring.datasource.password=Rocky.2004#

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Tests use mocked repositories, so the startup data seeder must not run.
app.seed-data=false
